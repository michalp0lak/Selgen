import numpy as np

path = 'path to the folder with images'
etalon_path = 'path where you put etalon.mat file, which is part of project'

lower_thresh = np.array([25,60,50])
upper_thresh = np.array([95,250,130])